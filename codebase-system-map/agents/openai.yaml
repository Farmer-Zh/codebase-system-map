interface:
  display_name: "Codebase System Map"
  short_description: "Shape indexed repository facts into a readable system map"
  default_prompt: "Use $codebase-system-map with an installed code-intelligence provider to generate a standalone HTML map of this repository, shaped for the reader and priorities in my request."
policy:
  allow_implicit_invocation: true
